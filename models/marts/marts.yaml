version: 2

models:
  - name: fct_analysis_crimes
    columns:
      - name: tokyo_crimes_id
        tests:
          - not_null
          - unique

  - name: dim_coordinate_masters
    columns:
      - name: address_num
        tests:
          - not_null
          - unique